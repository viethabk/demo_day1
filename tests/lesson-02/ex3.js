//Given: 

let a = true;
let b = false;
let c = true;

/*
Answer
1. a && b && c => return: FALSE (because 3 variable has different value so they can't match all conditions)
2. a && b || c => return: TRUE (because || is OR statement so we can return TRUE because a and c are TRUE)
*/